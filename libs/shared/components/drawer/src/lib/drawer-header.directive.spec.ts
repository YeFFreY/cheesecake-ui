import { DrawerHeaderDirective } from './drawer-header.directive';

describe('DrawerHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
