import { DrawerBodyDirective } from './drawer-body.directive';

describe('DrawerBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerBodyDirective();
    expect(directive).toBeTruthy();
  });
});
